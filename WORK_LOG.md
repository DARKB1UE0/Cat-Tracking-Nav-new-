# 猫车项目工作日志

### 20260917-03：同步 6020 顺时针 90° 安装零位

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-17（北京时间） |
| 记录人 | Codex |
| 类型 | 配置、文档、烧录记录 |
| 模块 | 云台安装零位、USB 反馈、相机坐标说明 |
| 状态 | 固件已烧录校验，物理朝向待确认 |
| 关联记录 | 下位机 `BasicFramework_F4/WORK_LOG.md` 的 20260917-03 |

#### 目标与背景

用户现场反馈原 yaw 零位朝左，要求顺时针修正 90°，需要同步默认保持位置与上位机角度基准。

#### 工作内容

- 下位机设置 yaw 安装零点为原始编码器角 -90°（等价 270°、约 6144），按当前左转为正约定实现顺时针 90°，达妙仍使用自身零点。
- 指令和状态均相对新安装零位，上位机只需发送 yaw=0 即要求保持新零位；Python 接口说明与协议文档已同步。
- 相机 TF 的安装偏角只填写残余偏差，避免重复叠加下位机已经处理的 90° 修正。

#### 验证与结果

- 固件完整 ARM 编译和 3 项 C/Python 模拟通过。
- 新固件经 ST-Link 写入，返回 `Verified OK` 并执行复位启动；烧录日志及哈希见下位机记录。
- 本次本地提交：下位机 `aae87d4`，RealSense 子仓库 `f85dc09`；主仓库同步子模块指针与中文日志，未推送远端。

#### 问题与限制

- 当前按代码坐标约定解释顺时针方向，电机实际安装方向、最终朝向及 ROS/TF 实物链路仍需现场确认。

#### 下一步

- [ ] 现场核对新零位朝向及 yaw 正负方向，再完成相机安装偏角标定。

### 20260917-02：下位机烧录连接检查与成功写入

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-17（北京时间） |
| 记录人 | Codex |
| 类型 | 烧录记录 |
| 模块 | C 板、ST-Link、SWD |
| 状态 | 烧录及校验成功，已执行复位启动 |
| 关联记录 | 下位机 `BasicFramework_F4/WORK_LOG.md` 的 20260917-02 |

#### 目标与背景

用户要求烧录电机零点保持固件，开始使用已连接的 ST-Link V3 检查目标。

#### 工作内容

- 确认固件文件及哈希，使用 OpenOCD 的 STM32F4/SWD 配置尝试连接。

#### 验证与结果

- ST-Link 枚举正常，但目标参考电压仅 0.449001 V，OpenOCD 无法连接目标。
- 前两次连接失败时未执行芯片擦除、固件写入或复位运行。
- 按用户要求重试：同一 ST-Link 测得目标参考电压 0.447403 V，仍无法连接目标；未擦除或写入，固件哈希与首次尝试一致。
- 用户排查后再次重试，电压恢复约 3.39 V，识别到 Cortex-M4、芯片 ID 0x10076413、1 MiB Flash。
- 已将 `11a0e70` 对应的电机零点保持固件写入 C 板，OpenOCD 返回 `Programming Finished`、`Verified OK` 并复位启动，退出码 0。具体文件哈希与原始日志路径见下位机关联记录。

#### 问题与限制

- 低参考电压问题已消失。烧录与校验成功不代表已验证电机动作或 ROS/USB 的完整实物链路；当前电脑尚未枚举出独立 C 板 USB CDC 设备。

#### 下一步

- [x] 连接恢复后完成烧录、校验并记录实际结果。
- [ ] 验证电机零点保持及 C 板 USB 上位机控制。

### 20260917-01：无上位机指令时保持电机零点

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-17（北京时间） |
| 记录人 | Codex |
| 类型 | 功能、协议说明、验证 |
| 模块 | ROS USB 桥接、云台默认行为、电机零位 |
| 状态 | 编译与模拟通过，待实机联调 |
| 关联记录 | 下位机 `BasicFramework_F4/WORK_LOG.md` 的 20260917-01 |

#### 目标与背景

用户要求无上位机信息时由下位机主动保持零点，并明确使用电机自身零点。本记录以最终电机零点实现为准，替换原先“无指令停机”和“开机姿态为零”的行为。

#### 工作内容

- 下位机默认以 GM6020 编码器 0、DM4310P 反馈 0 rad 为目标；GM6020 选择距开机角度最近的整圈零点以避免边界绕远。
- 上位机不发送或目标超过 100 ms 未更新时，默认回零保持；显式 STOP/mode=0 继续锁存停止，电机故障/失联仍优先停机。
- ROS 桥接启动和退出不自动发送 STOP，空闲不重放旧目标；有意停机须通过 `/gimbal/command` 的 enable=0 请求。
- 更新 Python 接口说明与中文协议文档，说明状态 enabled=1、fault=0x01 可同时出现，代表上位机目标缺失但下位机允许保持零位。
- 指令和反馈角度均相对电机零位，上位机 USB 帧格式保持兼容；新增接口行为回归测试。

#### 验证与结果

- 上位机 13 项测试通过，覆盖协议、ROS 指令/状态回调以及启动/退出不覆盖下位机默认行为。
- 下位机完整 ARM 编译 124 个源码单元通过；2 项生产 C/Python 模拟场景通过，验证非零启动回零、编码器跨圈、指令超时、显式停止及故障优先。
- 未执行真实 ROS 2、USB 或电机联调，未烧录。
- 本次本地提交：下位机 `11a0e70`，上位机 RealSense 子仓库 `a61d689`；主仓库同步日志及子模块指针，未推送远端。

#### 问题与限制

- 退出上位机节点现在允许下位机回零保持，需要失能时先发送显式 STOP；STOP 不会因没有后续指令自动解除。
- 电机零点与车体朝向需单独标定，不能继续把任意开机姿态解释为车体正前方。相机 TF 的安装偏角需随零位定义核对。
- 实际保持精度和负载能力需在实机上调整 PID 与输出限幅。

#### 下一步

- [ ] 烧录并验证上位机未运行、停止发布、退出节点与显式 STOP 的不同表现。
- [ ] 按电机零点完成云台与相机安装姿态标定。

### 20260916-02：同步用户确认的云台电机配置

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-16（北京时间） |
| 记录人 | Codex |
| 类型 | 配置、文档 |
| 模块 | 云台 USB 协议说明、CAN1 电机配置 |
| 状态 | 参数已同步，固件编译与模拟通过，实机待验证 |
| 关联记录 | 下位机 `BasicFramework_F4/WORK_LOG.md` 的 20260916-02 |

#### 目标与背景

用户确认 GM6020 ID=1，并提供达妙调试工具截图。按这些信息完成固件配置，更新此前文档中的待确认默认值。

#### 工作内容

- 两轴使用 CAN1、1 Mbit/s；GM6020 命令/反馈 ID 为 0x1FF/0x205，达妙为 0x001/0x000，ID 不冲突。
- 达妙配置为 MIT 模式，P_MAX=12.5 rad、V_MAX=30 rad/s、T_MAX=10 Nm；下位机负责 CAN 与量程换算，上位机继续使用现有角度指令接口。
- 更新 RealSense 子仓库 `docs/gimbal_usb_protocol.md`，标注参数来自用户截图，并区分协议量程和 1 Nm 应用输出限制。
- 日志继续保存在 nav 主目录，下位机记录保存在下位机目录。

#### 验证与结果

- 下位机完整 ARM 编译 124 个源码单元并链接成功。
- C/Python 综合模拟通过，新增验证 Master ID=0 的反馈接收、新量程的位置/速度/力矩换算，以及旧 Master ID 不刷新在线状态。
- 本次上位机只更新协议文档，无 ROS/Python 运行代码变化；未进行硬件烧录和真实 ROS/电机联调。
- 本次本地提交：下位机 `5becd55`，上位机 RealSense 子仓库 `d66966c`；主仓库同步子模块指针，未推送远端。

#### 问题与限制

- 截图 CAN Timeout=0，电机端掉线保护尚需核实；C 板软件超时停止不能代替电机自身保护。
- 零位、方向、PID 与机械范围仍需实机标定。按图配置完成不等于已经验证电机实际运动。

#### 下一步

- [ ] 烧录新固件后，通过 USB 状态与 CAN1 抓包验证两轴反馈。
- [ ] 完成小角度控制、停止/掉线验证及安装姿态标定。

### 20260916-01：核查并修复上位机到 CAN1 双电机控制链路

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-16（北京时间） |
| 记录人 | Codex |
| 类型 | 审查、修复、验证 |
| 模块 | ROS 2、USB CDC、CAN1 云台、双端协议 |
| 状态 | 软件测试通过，ROS 与实物联调待验证 |
| 关联记录 | 下位机 `BasicFramework_F4/WORK_LOG.md` 的 20260916-01 |

#### 目标与背景

用户确认 GM6020 与 DM4310P 都使用 CAN1，要求判断控制链路是否真正接通。审查发现旧下位机 pitch 初始化、电机任务、超时停止和 USB 收发存在阻断问题，因此此前“接口已整合”不能等同于“实物已经打通”。本记录补充实际修复与验证证据。

#### 工作内容

- 重写 `tools/gimbal_usb.py` 的流式解析，保留跨次读取的半帧，处理粘包、噪声、非法长度/版本和 CRC 错误；增加角度/状态合法性及串口短写检查。
- ROS 桥接使用非阻塞状态读取，订阅深度为 1、volatile；连接时先 STOP，指令明确区分设定点和停止，通信失败后停止转发且不自动恢复旧目标，退出时尝试 STOP 并释放串口。
- 不自动重发旧控制命令，使用端应以约 50 Hz 发布有效目标，停止发布后交由下位机 100 ms 看门狗处理。状态保留 `[yaw_deg, pitch_deg, enabled, fault, age_ms]` 格式。
- 下位机两轴统一 CAN1 并实际接入 DJI/DM 驱动；补齐控制、反馈、失联停机与 USB 缓存处理，详细变更见关联日志。
- 扩充 `docs/gimbal_usb_protocol.md`，明确帧格式、故障位、初始角度基准、默认电机参数、ROS 命令和实物联调判据。
- 日志继续保存在本主目录 `Cat-Tracking-Nav-new-/WORK_LOG.md`，未迁回 RealSense 子仓库；同步子模块到本次已验证修改。

#### 验证与结果

- 上位机运行 `python -m unittest discover -s tests -p test_gimbal_usb.py -v`：12 项通过，覆盖协议、每个分包边界、串口异常、ROS 回调指令校验、停止与状态发布。ROS 部分替换依赖进行回调模拟，没有运行真实 DDS/executor。
- 下位机 ARM 交叉编译 124 个源码单元并链接成功。
- C/Python 综合场景在隔离 WebAssembly 环境通过：实际 Python 客户端指令经实际 C USB/协议/应用/电机/CAN BSP 代码产生 CAN1 控制帧，模拟电机反馈经 USB 回到 Python；覆盖超时、停止和反馈异常等。
- 已验证软件层面双向路径：`ROS 回调 → Python 协议 → C 板 USB → 命令任务 → 云台 → CAN1 电机驱动`，反馈反向返回。无法用模拟结果证明 USB 实际枚举、电机实际运动或控制精度。
- 本次本地代码提交：上位机 RealSense 子仓库 `effdfc4`，下位机 `e79dac7`；均使用 `DARKB1UE0 <DARKB1UE0@qq.com>`，未推送远端。

#### 问题与限制

- 尚未烧录或进行真实 ROS 2、USB、CAN1 与电机联调。
- 6020 拨码 ID、达妙 Motor/Master ID、MIT 模式、P/V/T 量程仍待实物确认，集中配置已标明默认值尚未核实。
- 关节角改为相对开机编码器零位。要表示摄像头相对车体朝向，仍需对齐车头、校准正方向及安装偏移。
- 本次结论针对云台控制链路，不代表 RViz 三维标注已完成：当前缺少检测结果到 `/cat_target_camera` 的实际发布接入，TF 方向、反馈时效和图像时间同步仍需专项验证。

#### 下一步

- [ ] 确认电机配置，烧录固件并启动 ROS USB 节点，观察真实状态反馈。
- [ ] 以 50 Hz 发布小角度目标，核对 CAN1 抓包与两轴实测角度。
- [ ] 实测停止/掉线处理，完成云台零位、方向与 PID 标定。
- [ ] 接入三维目标发布并继续验证 RViz 的完整坐标链路。

## 2026-09-15：上位机 ROS 2 与下位机 USB 云台控制

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-15（北京时间） |
| 记录人 | Codex |
| 类型 | 功能 |
| 模块 | ROS 2、USB CDC、云台控制 |
| 状态 | 待验证 |
| 关联记录 | 下位机 `BasicFramework_F4/WORK_LOG.md` |

#### 目标与背景

通过大疆 C 板 USB CDC 虚拟串口，由 ROS 2 上位机发送 yaw、pitch 角度指令，并接收云台状态反馈。

#### 工作内容

- 固件端新增 USB 云台协议，使用 `AA 55` 帧头、版本、消息类型、序号、长度和 CRC16/IBM 校验。
- 支持设定点、使能、急停、Ping 和状态反馈；100 ms 未收到有效指令时自动停止。
- 上位机新增 `Cat-Tracking-RealSense/ros2_usb_gimbal.py` ROS 2 桥接节点。
- 节点订阅 `/gimbal/command`，消息为 `[yaw_deg, pitch_deg, enable]`。
- 节点发布 `/gimbal/status`，包含当前角度、使能状态、故障码和指令年龄。
- 新增 `tools/gimbal_usb.py`、`docs/gimbal_usb_protocol.md`，并在 `requirements.txt` 增加 `pyserial`。

#### 验证与结果

- 已完成代码和接口静态检查。
- 尚未在 ROS 2 Humble、真实 USB 串口和云台硬件上联调。

#### 问题与限制

- 需要确认 Linux 下实际 USB 设备名和串口权限。
- 下位机 pitch 轴仍需配置真实 DM4310P 的 CAN ID、控制模式和机械限位。

#### 下一步

- [ ] 在 ROS 2 环境安装依赖并启动 USB 桥接节点。
- [ ] 使用 `ros2 topic pub` 验证角度指令、状态回传和急停。
- [ ] 完成 yaw、pitch 单轴低功率测试及整机联调。

### 20260915-02：车体与云台空间坐标转换接口

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-15（北京时间） |
| 记录人 | Codex |
| 类型 | 功能 |
| 模块 | ROS 2、空间坐标、云台 USB |
| 状态 | 待验证 |
| 关联记录 | 下位机 `BasicFramework_F4/WORK_LOG.md` |

#### 目标与背景

统一上位机车体坐标和下位机云台角度定义，支持目标点到 yaw/pitch 的转换，并提供云台初始安装姿态与最终车身关系配置。

#### 工作内容

- 新增 `tools/gimbal_geometry.py`，约定车体坐标为 x 前、y 左、z 上。
- 实现车体向量到云台 yaw/pitch、云台角度反算车体视线向量、世界坐标目标转换。
- 增加 yaw/pitch 初始零位和方向符号配置，解决安装偏角和电机正方向差异。
- 下位机新增 `gimbal_geometry.h/.c`，并提供 `GimbalUsbSetInitialPose()` 接口。
- 下位机接口将安装初始姿态作为角度转换基准，最终输出仍使用 USB 协议中的角度单位（度）。

#### 验证与结果

- 已完成静态代码检查和接口对照；未连接真实 ROS、IMU、USB 和电机硬件。

#### 问题与限制

- 当前变换假设车体无横滚/俯仰；若车体姿态变化，需要接入完整 TF/quaternion 变换。
- 初始零位和方向符号必须通过实车低速点动标定。

#### 下一步

- [ ] 用实车测量 yaw/pitch 初始零位并写入配置。
- [ ] 接入 ROS TF，验证世界坐标目标转换。
- [ ] 完成目标点、云台角度和电机反馈的闭环联调。

### 20260915-03：双目目标位置在 RViz 地图标注

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-15（北京时间） |
| 记录人 | Codex |
| 类型 | 功能 |
| 模块 | RealSense 双目、ROS 2 TF、RViz |
| 状态 | 待验证 |
| 关联记录 | 20260915-02 |

#### 目标与背景

利用双目相机得到目标距离和相机坐标，在 RViz 的 `map` 坐标系中标注目标位置。

#### 工作内容

- 新增 `Cat-Tracking-RealSense/ros2_target_marker.py`。
- 节点订阅 `/cat_target_camera`（`geometry_msgs/msg/PointStamped`），点坐标应为相机光学坐标系，单位米。
- 通过 TF 查找相机坐标系到 `map` 的变换，并发布 `/target_marker`（`visualization_msgs/msg/Marker`）供 RViz 显示。
- 使用 18 cm 红色球体标注目标，1 秒无更新自动消失。

#### 验证与结果

- 已完成静态接口检查；未连接 RealSense、TF 树和实际 RViz。

#### 问题与限制

- 必须存在 `map -> ... -> camera_color_optical_frame` 的 TF 链。
- 双目节点需要发布带距离的三维点，不能只发布二维像素。

#### 下一步

- [ ] 将检测节点输出的像素中心和深度转换为相机三维点并发布 `/cat_target_camera`。
- [ ] 在 ROS 2 中启动节点并检查 RViz 标记位置。

### 20260915-04：整合云台姿态与双目目标地图坐标

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-15（北京时间） |
| 记录人 | Codex |
| 类型 | 功能 |
| 模块 | 云台 USB 状态、相机 TF、目标标注 |
| 状态 | 待验证 |
| 关联记录 | 20260915-03 |

#### 目标与背景

摄像头安装在云台上，视线方向随云台 yaw/pitch 改变，需要把下位机 USB 回传的云台姿态接入相机 TF，再将双目目标位置标注到 RViz 的 `map`。

#### 工作内容

- 修改 `ros2_target_marker.py`，订阅 `/gimbal/status` 获取 yaw/pitch。
- 动态发布 `base_link -> camera_link` TF，使摄像头朝向跟随云台角度。
- 保留 `/cat_target_camera` 三维点到 `map` 的 TF 转换和 `/target_marker` 发布。
- 相机安装平移默认使用 x=0.20 m、z=0.35 m，可通过参数调整。

#### 验证与结果

- 已完成代码级链路整合；尚未连接真实 USB、TF、RealSense 和 RViz 验证姿态方向。

#### 问题与限制

- 必须继续提供 `camera_link -> camera_color_optical_frame` 的固定光学坐标变换。
- yaw/pitch 正负方向和相机安装零位需要实车标定；当前四元数计算采用 yaw 绕 z、pitch 绕 y 的约定。

#### 下一步

- [ ] 发布并检查相机光学坐标系静态 TF。
- [ ] 用 `tf2_echo` 验证云台转动时 camera TF 方向。
- [ ] 由 RealSense 深度生成 `/cat_target_camera` 后检查 RViz 标记位置。

### 20260915-05：代码审查与坐标链路修正

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-15（北京时间） |
| 记录人 | Codex |
| 类型 | 修复 |
| 模块 | ROS 2 TF、USB 云台协议 |
| 状态 | 待验证 |
| 关联记录 | 20260915-04 |

#### 目标与背景

审查摄像头随云台转动的 TF 和 USB 状态链路，修复会导致 RViz 标记失败或状态帧解析失败的问题。

#### 工作内容

- ROS 节点显式声明相机平移、yaw/pitch 安装偏置参数。
- 增加 `camera_link -> camera_color_optical_frame` 固定光学坐标变换。
- 云台角度更新 `base_link -> camera_link` 动态 TF，并保留 `map` 目标点转换。
- 修复下位机 Ping 回复错误使用设定点结构的问题，统一回复状态帧长度。

#### 验证与结果

- 已完成静态审查；未执行 ROS 2 运行和实车验证。

#### 问题与限制

- 需要用实际安装方向标定 yaw/pitch 偏置及正负号。
- 如果系统已有其他节点发布同名 TF，应避免重复发布。
