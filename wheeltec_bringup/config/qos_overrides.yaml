# ROS2 QoS Override 配置
# 覆盖 /scan 话题的订阅 QoS，使用 best_effort 以兼容 pointcloud_to_laserscan
/slam_toolbox:
  ros__parameters:
    qos_overrides:
      /scan:
        subscription:
          reliability: best_effort
          durability: volatile
          history: keep_last
          depth: 10
